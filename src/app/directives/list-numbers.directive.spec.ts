import { ListNumbersDirective } from './list-numbers.directive';

describe('ListNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new ListNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
